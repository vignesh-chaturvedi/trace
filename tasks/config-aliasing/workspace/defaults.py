# The shipped defaults. Every loaded config starts from these.
DEFAULTS = {
    "server": {"host": "localhost", "port": 8080, "tags": ["default"]},
    "logging": {"level": "info", "handlers": ["stdout"]},
}
